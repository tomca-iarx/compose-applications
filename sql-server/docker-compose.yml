services:
  sql-server:
    image: mcr.microsoft.com/mssql/server
    restart: always
    ports:
      - "1433:1433"
    volumes:
      - ./data:/var/opt/mssql/data
    environment:
      SA_PASSWORD: tomcain1!
      ACCEPT_EULA: Y
